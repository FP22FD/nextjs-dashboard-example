# React Component Patterns

- Use functional components with hooks.
- Prefer `type Props` for props typing; `interface` only if extending.
- Keep components pure and avoid unnecessary state.
- Use clear, descriptive naming for components and props.
- Break down large components only if it improves readability.
- Reuse components whenever possible.
