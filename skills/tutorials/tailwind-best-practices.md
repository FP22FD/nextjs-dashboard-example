# Tailwind CSS Best Practices

- Keep classes inline unless reused often.
- Use semantic HTML elements.
- Prefer responsive utilities over custom media queries.
- Group related utilities for readability.
- Use Tailwind's color palette instead of hard-coded colors.
- Avoid over-complicated class strings; break into multiple lines if necessary.
