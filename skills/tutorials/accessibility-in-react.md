# Accessibility in React

- Use semantic HTML (`<header>`, `<main>`, `<footer>`, `<button>`).
- Add ARIA roles and labels when needed.
- Ensure color contrast is sufficient.
- Use `alt` for images, `aria-label` for icons.
- Test keyboard navigation and focus order.
- Avoid using `div` for interactive elements.
