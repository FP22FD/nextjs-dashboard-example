import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // disable the dev debug of Next.js indicators in the bottom right corner of the page
  // devIndicators: false,
};

export default nextConfig;
